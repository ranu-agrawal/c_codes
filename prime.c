#include<stdio.h>
int main(void)
{
    int primes[100] = {[0]=1,[1]=2};
    int p=3, count;
    while((p=3) || (p>3))
        for (int p=3; p<count; p=p+2)
        {
          p % primes[p]!= 0;
          printf("%d", p);
          return 1;
        }
        // else if
        // print("Whatever");
    return 0;

}
